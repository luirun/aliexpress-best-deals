<% if review.review_content.length <= 1 %>
  There is no reviews for this product :(
<% else %>
  <div class="container">
    <div class="row user-menu-container square">
      <div class="col-md-6 user-details">
        <div class="row coralbg white">
          <div class="col-md-6 no-pad">
            <div class="user-pad">
              <h3><%= review.username %></h3>
              <h4 class="white"><i class="fa fa-flag"></i> <%= review.user_country %></h4>
              <h4 class="white"><i class="fa fa-calendar"></i> <%= review.review_date %></h4>
            </div>
          </div>
          <div class="col-md-6 no-pad">
            <div class="user-image"></div>
          </div>
        </div>
        <div class="row overview">
          <div class="col-md-12 user-pad text-center">
            <b>Review:</b> <%= review.review_content %>
          </div>
        </div>
      </div>
      <div class="col-md-1 user-menu-btns">
        <div class="btn-group-vertical square" id="responsive<%= i %>">
          <a href="#" class="btn btn-block btn-default active">
            <i class="fa fa-camera fa-3x"></i>
          </a>
          <a href="#" class="btn btn-default">
            <i class="fa fa-handshake-o fa-3x"></i>
          </a>
          <a href="#" class="btn btn-default">
            <i class="fa fa-paper-plane fa-3x"></i>
          </a>
          <a href="#" class="btn btn-default">
            <i class="fa fa-envelope-o fa-3x"></i>
          </a>
        </div>
      </div>
      <div class="col-md-5 user-menu user-pad <%= i %>">
        <div class="user-menu-content active img">
          <ul class="user-menu-list">
            <% if review.review_photos.length > 10 %>
              <%= render "/products/show/photo_carousel", photos: review.review_photos, a: i %>
            <% else %>
              <h2>Photos:</h2>
              <h3>No photos in this review <i class="fa fa-folder-open"></i></h3>
            <% end %>
          </ul>
        </div>
        <div class="user-menu-content">
          <h3>You may also like:</h3>
          <ul class="user-menu-list">
            <% @recommended_products[0+i*2...2+i*2].each do |product| %>
              <li>
                <h4>
                  <%= link_to product_path(pretty_url_encode(product.productTitle)) do %>
                    <%= image_tag product.imageUrl, style: "width:140px;height:90px;" %> 
                    <small class="coral"><strong>ONLY <%= product.salePrice %> USD!</strong></small>
                  <% end %>
                </h4>
              </li>
            <% end %>
          </ul>
        </div>
        <div class="user-menu-content">
          <h3>Popular Reviews:</h3>
          <div class="row">
            <% @product_reviews[0+i...2+i].each do |product_review| %>
              <div class="col-md-6">
                <div class="view">
                  <div class="caption">
                    <p><%= product_review.user_id %></p>
                    <%= link_to review_path(pretty_url_encode(product_review.title)), rel: "tooltip", title: "View" do %>
                      <span class="fa fa-search fa-2x"></span>
                    <% end %>
                  </div>
                  <%= image_tag product_review.cover.url, class: "img-responsive" %>
                </div>
                <div class="info">
                  <p class="small" style="text-overflow: ellipsis"><%= pretty_url_decode(product_review.title) %></p>
                  <p class="small coral text-right">
                    <i class="fa fa-clock-o"></i>
                    <%= distance_of_time_in_words(product_review.created_at, 0.1.minutes.from_now)%> ago
                  </p>
                </div>
                <div class="stats turqbg">
                  <span class="fa fa-heart-o"> <strong><%= rand(21..147) %></strong></span>
                  <span class="fa fa-eye pull-right"> <strong><%= rand(147..1555) %></strong></span>
                </div>
              </div>
            <% end %>
          </div>
        </div>
        <div class="user-menu-content">
          <div class="share-links">
            <center>
              <button type="button" class="btn btn-lg btn-labeled btn-warning" href="#">
                    <span class="btn-label"><i class="fa fa-bell-o"></i></span>A WORK IN PROGRESS
              </button>
            </center>
          </div>
        </div>
      </div>
    </div>
  </div>

    <script type="text/javascript">
    $(document).ready(function() {
        var $btnSets = $('#responsive<%= i %>'),
        $btnLinks = $btnSets.find('a');
     
        $btnLinks.click(function(e) {
            e.preventDefault();
            $(this).siblings('a.active').removeClass("active");
            $(this).addClass("active");
            var index = $(this).index();
            $("div.user-menu.<%= i %>>div.user-menu-content").removeClass("active");
            $("div.user-menu.<%= i %>>div.user-menu-content").eq(index).addClass("active");
        });
    });

    $( document ).ready(function() {
        $("[rel='tooltip']").tooltip();    
     
        $('.view').hover(
            function(){
                $(this).find('.caption').slideDown(250); //.fadeIn(250)
            },
            function(){
                $(this).find('.caption').slideUp(250); //.fadeOut(205)
            }
        ); 
    });
    </script>
<% end %>