<%= form_for(@subsubcategory, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @subsubcategory.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@subsubcategory.errors.count, "error") %> prohibited this subsubcategory from being saved:</h4>

      <ul>
      <% @subsubcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :parent, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.hidden_field :parent, :value => 30 %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
